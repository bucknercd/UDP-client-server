This is a simple UDP client-server setup which runs on port 10000. The udp server will receive a message 
from the udp client and will simply echo the message to stdout saying that it 
was received.

To make server and client:
$ make

To run server:
$ make server

To run client:
$ make client
